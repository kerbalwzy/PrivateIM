syntax = "proto3";

package pb;

// protoc -I ./ ./UserAuth.proto --go_out=plugins=grpc:./

service UserAuth {
    rpc CheckAuthToken (AuthToken) returns (TokenCheckResult);
}


message AuthToken {
    string data = 1;
}

message TokenCheckResult {
    int64 user_id = 1;
}


